import React from "react";;

const Title = () =>{
    console.log('Rendering Title..!');
    return(
        <><p>Usecallback Hook</p>
        <p>Usecallback is a Hook that will return memorizied version of the callback function that only changes or executes or run if one of dependencies has changed. </p>
        </>
    )
}
export default Title;